# Mitigation Guidelines

- Enforce signed memory entries with provenance checks.
- Disable dangerous tools by default and require scoped approvals.
- Perform prompt linting before and after summarization.
- Add exfiltration detectors on model outputs.
- Use context budget governance to prevent cognitive overload attacks.
